# Android 正式签名密钥配置模板（P0-5）
#
# 1. 复制本文件为同目录下的 key.properties（已被 .gitignore 忽略，绝不入库）：
#      cp key.properties.example key.properties
# 2. 生成正式 keystore（一次性，妥善备份，丢失后无法发布同包名升级）：
#      keytool -genkey -v -keystore ~/linli-release.jks \
#        -keyalg RSA -keysize 2048 -validity 10000 -alias linli
# 3. 按下面字段填入，store.file 用绝对路径或相对 android/ 的路径：
#
# 上架 Google Play 建议改用 Play App Signing（上传密钥 + 签名密钥分离），
# 并把密钥放进 CI 的受保护密钥库，而不是开发者本机。

store.file=/absolute/path/to/linli-release.jks
store.password=你的keystore密码
key.alias=linli
key.password=你的key密码
